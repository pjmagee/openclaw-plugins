{
  "name": "openclaw-plugins-workspace",
  "private": true,
  "description": "CI/dev tooling for the plugins in this repo — the plugins themselves are packed from plugins/<id>",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "build": "node scripts/build.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
